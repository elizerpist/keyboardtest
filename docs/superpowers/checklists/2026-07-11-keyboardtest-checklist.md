# Keyboardtest Acceptance Checklist

| ID | Source instruction | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| KT-001 | User: create new repo named `keyboardtest` from local `flutteetest`; save beside `exptv2` | Project root | Project exists at `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/keyboardtest` with package/repo identity updated to `keyboardtest`. | Direct file inspection of path, `pubspec.yaml`, Android namespace/applicationId, and git remote. | DONE |
| KT-002 | User: empty Flutter app with a FAB button | `lib/main.dart` | Initial screen is a minimal app with one FAB that opens the test sheet. | Flutter widget test verifies FAB exists before opening. | DONE |
| KT-003 | User: FAB opens a slide up sheet | `lib/main.dart` | Tapping the FAB reveals a bottom slide-up sheet. | Flutter widget test taps FAB and finds the sheet. | DONE |
| KT-004 | User: sheet contains a text pill above the safety zone | `lib/main.dart` | The sheet has a bottom text-entry pill positioned above the bottom safe area when the keyboard is closed. | Flutter widget test inspects pill and sheet widgets. | DONE |
| KT-005 | User: keyboard slides up and the pill follows; pill is a separate component | `lib/main.dart` | The pill is implemented as its own floating component and changes vertical position from `MediaQuery.viewInsets.bottom`; the sheet body is not moved by the keyboard. | Flutter widget test pumps fake viewInsets and verifies the pill moves while the sheet remains present. | DONE |
| KT-006 | User: upload to GitHub as `keyboardtest` | Git/GitHub | GitHub repo `elizerpist/keyboardtest` exists and main branch contains the implementation commit. | `gh repo view elizerpist/keyboardtest` and `git status`. | DONE |
| KT-007 | User: build online; build name includes commit name | `.github/workflows/android-build.yml` | GitHub Actions builds debug APK and uploads an artifact whose name includes the short commit SHA. | `gh run` inspection and artifact name. | DONE |
| KT-008 | User: download to Android `/emulated/0/download/keyboardtest`; folder does not exist yet | Android shared storage | The built APK artifact is downloaded and copied under `/storage/emulated/0/Download/keyboardtest`. | `ls -la /storage/emulated/0/Download/keyboardtest`. | DONE |
| KT-009 | User: "arra kértelek, hogy a sheet egyszerre mozogjon a pillel. a slide up sheet. nem más, nem extra container." | `lib/main.dart` | The actual slide-up sheet and the floating pill move together from the same shared transform; no extra sheet-backplate/container layer is rendered. | Widget test pumps fake viewInsets, verifies the sheet and pill move from their closed positions by the same delta, and verifies `keyboardtest-sheet-backplate` is absent. | DONE |
| KT-010 | User: "amikor visszaslideol le, akkor kicsit lejjebb kerül, és utána visszapattan" | `lib/main.dart` | When keyboard inset returns to zero, the pill never docks below the safe-area bottom position. | Widget test pumps keyboard open then zero inset and verifies pill bottom equals `safeBottom + spacing`, with no lower fallback. | DONE |
| KT-011 | User: "nem a kért onscreen debug panelt kaptam... egy gomb van balnoldalt, ami megnyitja... másolás vágólapra gomb is, exptv2-ben van benne." | `lib/main.dart` | The debug UI matches the `exptv2` pattern: a left floating terminal button opens a dialog with copy-to-clipboard, clear, close, and log text. The fixed top overlay panel is removed. | Widget test verifies `debug-floating-button`, opens `debug-console-dialog`, taps `debug-console-copy`, and captures clipboard text. | DONE |
| KT-012 | User: inspect synchronized motion via debug | `lib/main.dart` | Debug logs include raw inset, safe bottom, dock bottom, keyboard lift, sheet translation, and pill bottom. | Widget test opens the debug dialog and checks copied log text contains those fields. | DONE |
| KT-013 | User: "néha van egy minimális lag... valahogy mérni kéne... még több debug logot kérek" | `lib/main.dart` | Copied debug logs include motion sequence, per-sample delta timing, raw inset delta, velocity, build counters, and frame timing field labels for jank investigation. | Widget test opens the debug dialog, copies logs, and checks diagnostic fields. | DONE |
| KT-014 | User: "ha a sheet tele van funkcionális contenttel, akkor nem fog e laggolni?" | `lib/main.dart` | Keyboard motion is isolated with repaint boundaries and batched debug notifications so sheet content is easier to diagnose and less likely to repaint from log updates. | Widget test verifies repaint boundary keys for moving layer, sheet, and pill, and debug notifier batching is covered by copied diagnostic logs. | DONE |
| KT-015 | User: "néha smooth, néha nem... pár frame lag... konziszensen legyen jó" plus pasted frame logs after close | `lib/main.dart` | Debug console updates do not explicitly schedule new frames, and frame timing logs are rate-limited/aggregated so diagnostics cannot create a self-sustaining jank loop while the dialog is open. | Widget test verifies copied diagnostics expose frame rate-limit fields and code inspection verifies `_scheduleNotify()` does not call `scheduleFrame()`. | DONE |
| KT-016 | User: "pár frame lag... valahogy mérni kéne" | `lib/main.dart` | Motion diagnostics expose target keyboard lift, visual lift, lag in pixels, and motion source so jitter can be separated from real sheet/pill desync. | Widget test opens the debug dialog, copies logs, and checks `targetLift=`, `visualLift=`, `lagPx=`, and `source=` fields. | DONE |
| KT-017 | User: "konzisztensen legyen jó" | `lib/main.dart` | The actual slide-up sheet and pill remain in one shared transform while the visual lift uses a short bounded catch-up for raw IME sample jitter; no extra pill container or backplate is added. | Widget test pumps fake insets through the settling duration, verifies sheet and pill still move by the same delta, and verifies no `keyboardtest-sheet-backplate` exists. | DONE |
| KT-018 | User: "`keyboard-smooth-v1` legyen mérföldkő; nézd meg még mit tudsz javítani" | `lib/main.dart` | The smooth shared motion implementation is preserved while debug-only improvements are added. | Code inspection verifies `_catchupDuration` remains 18 ms and the sheet/pill still share one transform. | DONE |
| KT-019 | Assistant proposal approved by user: debug console render cost | `DebugConsoleDialog` in `lib/main.dart` | The dialog renders only a tail of recent log lines, while the copy button still copies the full log history. | Widget test fills more than the visible tail count, verifies old lines are hidden from `debug-console-text`, and verifies copy includes old and recent lines. | DONE |
| KT-020 | Assistant proposal approved by user: clearer jank labels | `DebugConsole.logMotion`, `DebugPerformanceProbe` in `lib/main.dart` | Motion logs distinguish `idleGap`, `sampleGap`, `activeMotion`, and `motionPhase`; frame logs use `source=flutterFrame` plus `debugOpen=` instead of implying the dialog caused the frame. | Widget test verifies copied diagnostics contain the new fields. | DONE |
