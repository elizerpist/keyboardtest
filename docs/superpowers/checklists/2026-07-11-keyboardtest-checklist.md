# Keyboardtest Acceptance Checklist

| ID | Source instruction | Intended code area | Acceptance condition | Verification method | Status |
| --- | --- | --- | --- | --- | --- |
| KT-001 | User: create new repo named `keyboardtest` from local `flutteetest`; save beside `exptv2` | Project root | Project exists at `/data/data/com.termux/files/home/ubuntu/flutteruser/flutterapps/keyboardtest` with package/repo identity updated to `keyboardtest`. | Direct file inspection of path, `pubspec.yaml`, Android namespace/applicationId, and git remote. | DONE |
| KT-002 | User: empty Flutter app with a FAB button | `lib/main.dart` | Initial screen is a minimal app with one FAB that opens the test sheet. | Flutter widget test verifies FAB exists before opening. | DONE |
| KT-003 | User: FAB opens a slide up sheet | `lib/main.dart` | Tapping the FAB reveals a bottom slide-up sheet. | Flutter widget test taps FAB and finds the sheet. | DONE |
| KT-004 | User: sheet contains a text pill above the safety zone | `lib/main.dart` | The sheet has a bottom text-entry pill positioned above the bottom safe area when the keyboard is closed. | Flutter widget test inspects pill and sheet widgets. | DONE |
| KT-005 | User: keyboard slides up and the pill follows; pill is a separate component | `lib/main.dart` | The pill is implemented as its own floating component and changes vertical position from `MediaQuery.viewInsets.bottom`; the sheet body is not moved by the keyboard. | Flutter widget test pumps fake viewInsets and verifies the pill moves while the sheet remains present. | DONE |
| KT-006 | User: upload to GitHub as `keyboardtest` | Git/GitHub | GitHub repo `elizerpist/keyboardtest` exists and main branch contains the implementation commit. | `gh repo view elizerpist/keyboardtest` and `git status`. | DONE |
| KT-007 | User: build online; build name includes commit name | `.github/workflows/android-build.yml` | GitHub Actions builds debug APK and uploads an artifact whose name includes the short commit SHA. | `gh run` inspection and artifact name. | DONE |
| KT-008 | User: download to Android `/emulated/0/download/keyboardtest`; folder does not exist yet | Android shared storage | The built APK artifact is downloaded and copied under `/storage/emulated/0/Download/keyboardtest`. | `ls -la /storage/emulated/0/Download/keyboardtest`. | DONE |
| KT-009 | User: "arra kértelek, hogy a sheet egyszerre mozogjon a pillel. a slide up sheet. nem más, nem extra container." | `lib/main.dart` | The actual slide-up sheet and the floating pill move together from the same shared transform; no extra sheet-backplate/container layer is rendered. | Widget test pumps fake viewInsets, verifies the sheet and pill move from their closed positions by the same delta, and verifies `keyboardtest-sheet-backplate` is absent. | DONE |
| KT-010 | User: "amikor visszaslideol le, akkor kicsit lejjebb kerül, és utána visszapattan" | `lib/main.dart` | When keyboard inset returns to zero, the pill never docks below the safe-area bottom position. | Widget test pumps keyboard open then zero inset and verifies pill bottom equals `safeBottom + spacing`, with no lower fallback. | DONE |
| KT-011 | User: "nem a kért onscreen debug panelt kaptam... egy gomb van balnoldalt, ami megnyitja... másolás vágólapra gomb is, exptv2-ben van benne." | `lib/main.dart` | The debug UI matches the `exptv2` pattern: a left floating terminal button opens a dialog with copy-to-clipboard, clear, close, and log text. The fixed top overlay panel is removed. | Widget test verifies `debug-floating-button`, opens `debug-console-dialog`, taps `debug-console-copy`, and captures clipboard text. | DONE |
| KT-012 | User: inspect synchronized motion via debug | `lib/main.dart` | Debug logs include raw inset, safe bottom, dock bottom, keyboard lift, sheet translation, and pill bottom. | Widget test opens the debug dialog and checks copied log text contains those fields. | DONE |
| KT-013 | User: "néha van egy minimális lag... valahogy mérni kéne... még több debug logot kérek" | `lib/main.dart` | Copied debug logs include motion sequence, per-sample delta timing, raw inset delta, velocity, build counters, and frame timing field labels for jank investigation. | Widget test opens the debug dialog, copies logs, and checks diagnostic fields. | DONE |
| KT-014 | User: "ha a sheet tele van funkcionális contenttel, akkor nem fog e laggolni?" | `lib/main.dart` | Keyboard motion is isolated with repaint boundaries and batched debug notifications so sheet content is easier to diagnose and less likely to repaint from log updates. | Widget test verifies repaint boundary keys for moving layer, sheet, and pill, and debug notifier batching is covered by copied diagnostic logs. | DONE |
| KT-015 | User: "néha smooth, néha nem... pár frame lag... konziszensen legyen jó" plus pasted frame logs after close | `lib/main.dart` | Debug console updates do not explicitly schedule new frames, and frame timing logs are rate-limited/aggregated so diagnostics cannot create a self-sustaining jank loop while the dialog is open. | Widget test verifies copied diagnostics expose frame rate-limit fields and code inspection verifies `_scheduleNotify()` does not call `scheduleFrame()`. | DONE |
| KT-016 | User: "pár frame lag... valahogy mérni kéne" | `lib/main.dart` | Motion diagnostics expose target keyboard lift, visual lift, lag in pixels, and motion source so jitter can be separated from real sheet/pill desync. | Widget test opens the debug dialog, copies logs, and checks `targetLift=`, `visualLift=`, `lagPx=`, and `source=` fields. | DONE |
| KT-017 | User: "konzisztensen legyen jó" | `lib/main.dart` | The actual slide-up sheet and pill remain in one shared transform; no extra pill container or backplate is added. Current active IME samples apply the target lift directly per KT-024. | Widget tests verify settled and immediate fake inset samples move sheet and pill by the same delta, and verify no `keyboardtest-sheet-backplate` exists. | DONE |
| KT-018 | User: "`keyboard-smooth-v1` legyen mérföldkő; nézd meg még mit tudsz javítani" | Git/tag plus `lib/main.dart` | The `keyboard-smooth-v1` tag preserves the earlier smooth-motion milestone, while current `main` may supersede catch-up behavior when it causes measured lag. | `git tag --points-at 61cb8b0` verifies the milestone tag, and widget tests verify the sheet/pill still share one transform. | DONE |
| KT-019 | Assistant proposal approved by user: debug console render cost | `DebugConsoleDialog` in `lib/main.dart` | The dialog renders only a tail of recent log lines, while the copy button still copies the full log history. | Widget test fills more than the visible tail count, verifies old lines are hidden from `debug-console-text`, and verifies copy includes old and recent lines. | DONE |
| KT-020 | Assistant proposal approved by user: clearer jank labels | `DebugConsole.logMotion`, `DebugPerformanceProbe` in `lib/main.dart` | Motion logs distinguish `idleGap`, `sampleGap`, `activeMotion`, and `motionPhase`; frame logs use `source=flutterFrame` plus `debugOpen=` instead of implying the dialog caused the frame. | Widget test verifies copied diagnostics contain the new fields. | DONE |
| KT-021 | User: "javítsd az észrevételeket" after logs showing active `sampleGap=true`, warmup frame, and debug dialog frame cost | `KeyboardMotionLayer`, `KeyboardMotionMetrics`, `DebugPerformanceProbe` in `lib/main.dart` | Active sample gaps are exposed as `bridgeMs`/`bridgedGap` diagnostics without adding a new motion layer; KT-024 supersedes visual catch-up by applying active samples directly. | Widget test verifies copied logs contain bridge diagnostics and existing sheet/pill shared motion tests still pass. | DONE |
| KT-022 | User: "javítsd az észrevételeket" after logs showing high initial build frames | `DebugPerformanceProbe` in `lib/main.dart` | Frame timing diagnostics label first-frame warmup rows with `warmupFrame=` so startup/focus warmup is separated from steady-state jank. | Widget test verifies copied diagnostics contain `warmupFrame=`. | DONE |
| KT-023 | User: "javítsd az észrevételeket" after logs showing `debugOpen=true buildMs=102.9` | `DebugConsoleDialog` in `lib/main.dart` | The dialog log view renders as lightweight text rather than a read-only `TextField`, while the copy button still exports the full log. | Widget test verifies the debug dialog contains no descendant `TextField` for log output, the tail text is visible, and copy still includes full history. | DONE |
| KT-024 | User: "nem mindig tökéletes, és lefele slide során a keyboard gyorsabb" with logs showing large closing `lagPx` during `sampleGap=true` | `KeyboardMotionLayer` in `lib/main.dart` | During active IME samples, the shared sheet+pill transform applies the current keyboard target in the same frame, especially on downward keyboard motion, so the layer is not one raw inset sample behind the keyboard. | Widget test pumps opening and closing fake IME samples without settling time and verifies sheet and pill immediately match the new target lift from the same transform. | DONE |
| KT-025 | User: "az első slide darabos kicsit, a többi kevésbé" | Text input warmup in `lib/main.dart` | The platform text input path is prewarmed before the first visible pill focus, without requesting pill focus or showing the keyboard. | Widget test verifies sheet-open logs report `text input prewarm ready=true` before any `focus active=true` line. | DONE |
| KT-026 | User: "mindet, javítsd meg, azt akarom outputként hogy egy jól teljesítő app legyen" | `lib/main.dart` performance probe | Frame diagnostics report `buildDuration`, `rasterDuration`, `totalSpan`, `vsyncOverhead`, display refresh rate, and an observed-vsync-derived frame budget instead of classifying `build + raster` with a fixed 16.7 ms threshold. | Unit/widget tests for frame-budget math and copied diagnostic fields; direct code inspection against Flutter `FrameTiming`. | DONE |
| KT-027 | User: "mindet, javítsd meg" after diagnosis of mixed inset/build/raster timing | `lib/main.dart` metrics observer and diagnostics | Diagnostics separately timestamp metrics arrival, transform application, and completed frame timing using a monotonic clock. | Widget/unit tests inject ordered timestamps and verify distinct latency fields; physical profile report inspection. | PARTIAL |
| KT-028 | User: "mindet, javítsd meg" after diagnosis that per-sample string logging can perturb measurements | `lib/main.dart` debug data store and dialog | Keyboard-motion hot-path collection uses a bounded structured ring buffer and does not format the full readable log until display/copy time; collection can be disabled for A/B measurement. | Unit tests verify capacity, deferred formatting, and enabled/disabled behavior; profile A/B comparison. | PARTIAL |
| KT-029 | User: "azt akarom outputként hogy egy jól teljesítő app legyen" | `KeyboardMotionLayer` and moving visual subtree in `lib/main.dart` | Only the shared transform owner reacts to IME metrics; sheet and pill visual content remain stable children, retain repaint isolation, and authoritative inset samples have absolute `lagPx <= 1.0`. | Widget tests verify shared movement and stable subtree build counts; physical profile report verifies lag. | PARTIAL |
| KT-030 | User: "azt akarom outputként hogy egy jól teljesítő app legyen" | Profile APK and physical-device performance report | After one warmup cycle, ten keyboard open/close cycles have UI-build and raster p95 no greater than one observed frame budget, no steady-state UI/raster frame over two budgets, and diagnostics overhead no greater than 10% p95. | Two physical-device profile captures with diagnostics disabled/enabled and debug dialog closed. | NOT DONE |
| KT-031 | User approved all prior recommendations, including gap handling only after correct profile measurement | `KeyboardMotionLayer` in `lib/main.dart` | Direct target synchronization remains default; if profile evidence shows external metrics gaps above 1.5 vsync intervals with in-budget Flutter frames, any predictor is limited to one vsync, clamped to endpoints, and introduces no authoritative-sample lag above 1 logical pixel. | Profile evidence records the decision; predictor unit/widget tests are required if enabled. | NOT DONE |
| KT-032 | User: "ne tömörített artifactként buildeljen, hanem egy download linket kapj" | `.github/workflows/android-build.yml` | Online CI builds `keyboardtest-<short-sha>-profile.apk`, uploads it directly as a commit-specific GitHub Release asset, publishes its direct URL, and does not deliver the APK through `actions/upload-artifact`. | Workflow contract test, successful GitHub Actions run, Release asset metadata, and direct HTTP response. | DONE |
| KT-033 | User: "ha kész az online build, az emulated 0 download keyboardtest mappábba helyezd" | GitHub Release plus `/storage/emulated/0/Download/keyboardtest` | After the online profile build succeeds, the direct APK asset is downloaded to the requested Android directory with its SHA-bearing filename and a matching non-empty SHA-256 digest. | `gh release view`, direct download, `ls -l`, `file`, and `sha256sum` comparison. | DONE |
